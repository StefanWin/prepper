// Import {stringify} from '../util';
import {DependenciesConfig} from './interface';

export const checkDependencies = (_config: DependenciesConfig) => {
	// Stringify(config);
};

export * from './interface';
